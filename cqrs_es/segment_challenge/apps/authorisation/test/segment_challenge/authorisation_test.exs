defmodule SegmentChallenge.AuthorisationTest do
  use ExUnit.Case
  doctest SegmentChallenge.Authorisation

  test "greets the world" do
    assert SegmentChallenge.Authorisation.hello() == :world
  end
end
