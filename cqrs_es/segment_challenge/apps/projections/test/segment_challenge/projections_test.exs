defmodule SegmentChallenge.ProjectionsTest do
  use ExUnit.Case
  doctest SegmentChallenge.Projections

  test "greets the world" do
    assert SegmentChallenge.Projections.hello() == :world
  end
end
